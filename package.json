{
  "name": "astroad",
  "description": "A pre-configured setup for easy website development with Astro and Payload CMS using Docker.",
  "license": "MIT",
  "version": "1.1",
  "scripts": {
    "dev": "concurrently \"cd astro && yarn dev\" \"cd payload && yarn dev\"",
    "format": "cd astro && yarn format && cd .. && cd payload && yarn format && cd .."
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
