{
  "name": "for-the-records",
  "description": "My record collection, digitalized.",
  "license": "MIT",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd astro && yarn dev\" \"cd payload && yarn dev\"",
    "format": "cd astro && yarn format && cd .. && cd payload && yarn format && cd .."
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
